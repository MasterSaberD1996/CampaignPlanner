import { AnimatedLabelDirective } from './animated-label.directive';

describe('AnimatedLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimatedLabelDirective();
    expect(directive).toBeTruthy();
  });
});
